<li class="settings-panel--setting settings-panel--{{setting}} settings-panel--input-binary">
    <div class="settings-panel--description">
        <label>{{!name}}{{!help}}</label>
        {{!small}}
        {{!reset}}
    </div>
    <input-binary name="{{setting}}" value="{{value}}">
        {{!options}}
    </input-binary>
</li>
